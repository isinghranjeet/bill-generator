- [x] Verify backend invoice route mount (/api + /invoices => /api/invoices)
- [x] Verify client invoice API uses /api/invoices
- [ ] Fix runtime config so requests no longer hit `/invoices` (missing `/api`)
- [ ] Remove any remaining hardcoded /invoices base (if present)
- [ ] Re-run dev and validate via Network tab

